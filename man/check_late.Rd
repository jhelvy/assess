% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/github.R
\name{check_late}
\alias{check_late}
\title{Report student work committed after an assignment's deadline}
\usage{
check_late(
  roster,
  due,
  assign = NULL,
  dir = here::here("repos"),
  repo_col = "gh",
  instructor = "john.helveston@gmail.com"
)
}
\arguments{
\item{roster}{Course roster data frame with \code{netID}, \code{enrolled}, and
\code{repo_col} columns; \code{name}, if present, is carried into the result.}

\item{due}{Data frame with \code{assign} and \code{due} (POSIXct) columns, from
\code{\link[=read_due_dates]{read_due_dates()}} or an \code{assignments} table that carries a \code{due} column.
Row order sets the report order.}

\item{assign}{Assignment(s) to check: either a character vector of names, or
a \code{pars} list (its \verb{$assign} element is used), so you can pass the \code{pars}
you are grading with and see only that assignment. Defaults to \code{NULL},
which sweeps every assignment whose deadline has passed -- so a late \code{hw1}
landing while you grade \code{hw3} still surfaces.}

\item{dir}{Directory containing the local clones, e.g.
\code{here::here("repos")}.}

\item{repo_col}{Roster column holding each repo's exact name, also used as
the clone's folder name. Defaults to \code{"gh"}.}

\item{instructor}{Instructor's git author email, whose commits are excluded.
Match on email, not name: the same person often commits under more than one
author name.}
}
\value{
Invisibly, a tibble with one row per student and assignment with late
activity: \code{netID}, \code{name}, \code{assign}, \code{n_commits}, \code{due}, \code{last_commit},
\code{hours_late}, \code{after_feedback}, and \code{files}.
}
\description{
Run right after \code{\link[=pull_repos]{pull_repos()}}, before grading, to see what arrived late.
Two things are flagged, and neither alone is enough:
}
\details{
\itemize{
\item \code{hours_late} -- committed past the deadline. This is the primary signal,
and it is what catches the grading window: a typical \code{grade.R} pulls again
immediately before \code{\link[=push_repos]{push_repos()}}, so a student who pushes while you are
reviewing gets swept in \emph{underneath} the feedback commit and would
otherwise look like they had been there all along.
\item \code{after_feedback} -- the commit arrived after that assignment's feedback was
pushed, so it definitely was not reviewed. \code{FALSE} means it was already in
the tree when you graded (it may or may not have been seen); \code{NA} means the
assignment has not been graded yet.
}

The anchor for \code{after_feedback} is the last commit touching
\verb{feedback/<assign>.md} in that repo, which is written only when feedback is
delivered -- so no snapshot file or extra state is needed.

This is a read-only report: nothing is written and no git state is touched.
Applying the course late policy stays a manual decision.
}
