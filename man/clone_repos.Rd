% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/github.R
\name{clone_repos}
\alias{clone_repos}
\title{Clone each student repo that isn't already on disk}
\usage{
clone_repos(roster, org, dir = here::here(), repo_col = "gh", dry_run = FALSE)
}
\arguments{
\item{roster}{Course roster data frame with \code{netID}, \code{enrolled}, and
\code{repo_col}.}

\item{org}{GitHub organization the repos live in (e.g. \code{"eda-f26"}).}

\item{dir}{Directory to clone into. Defaults to \code{\link[here:here]{here::here()}}; pass the
folder that holds the clones (e.g. \code{here::here("repos")}).}

\item{repo_col}{Roster column holding each repo's exact name, also used as
the local clone folder name under \code{dir}. Defaults to \code{"gh"}.}

\item{dry_run}{If \code{TRUE}, print what would be cloned without calling \code{gh}.}
}
\value{
Invisibly, a tibble with one row per enrolled student and a
\code{status} column (\code{cloned}, \code{exists}, \code{failed}, or \code{would_clone}).
}
\description{
Clones every enrolled student's repo from \code{org} into \verb{dir/<repo_col>}.
Repos already cloned are left completely alone -- nothing is fetched,
committed, or overwritten -- so this is safe to re-run whenever students are
added mid-semester.
}
\details{
This is the only function that creates local clones. \code{\link[=push_repos]{push_repos()}} and
\code{\link[=pull_repos]{pull_repos()}} both skip repos that aren't on disk yet; run this first.

Requires the \code{gh} CLI to be installed and authenticated
(\verb{gh auth login}), and the repos to already exist on GitHub (see
\code{\link[=create_repos]{create_repos()}}).
}
