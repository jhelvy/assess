% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/github.R
\name{pull_repos}
\alias{pull_repos}
\title{Pull student work into every local clone}
\usage{
pull_repos(roster, dir = here::here(), repo_col = "gh", dry_run = FALSE)
}
\arguments{
\item{roster}{Course roster data frame with \code{netID}, \code{enrolled}, and
\code{repo_col}.}

\item{dir}{Directory containing the local clones. Defaults to
\code{\link[here:here]{here::here()}}; pass the folder that holds them (e.g.
\code{here::here("repos")}).}

\item{repo_col}{Roster column holding each repo's exact name, also used as
the local clone folder name under \code{dir}. Defaults to \code{"gh"}.}

\item{dry_run}{If \code{TRUE}, report which repos are behind without rebasing.}
}
\value{
Invisibly, a tibble with one row per enrolled student and a
\code{status} column (\code{pulled}, \code{uptodate}, \code{skipped}, \code{conflict}, \code{failed},
or \code{would_pull}).
}
\description{
Fetches each enrolled student's repo and rebases the local clone onto it, so
the clones hold whatever students have pushed since you last looked.
}
\details{
Run this before \code{\link[=push_repos]{push_repos()}}, every time. \code{push_repos()} only stages,
commits, and pushes -- it never fetches -- so the moment a student commits
their own work the clone is behind and the push is rejected with \emph{"Updates
were rejected because the remote contains work that you do not have
locally"}.

Rebase (not merge) keeps the history linear: local commits not yet pushed
(feedback PDFs, refreshed starter files) replay on top of the student's
work. Uncommitted changes are stashed and restored around the rebase
(\code{--autostash}). Instructor and student rarely touch the same file, so
conflicts are unusual; a repo that does conflict is left mid-rebase and
reported as \code{conflict} for you to resolve by hand.

Purely local git plus a fetch from each clone's own \code{origin}, so no \code{org}
argument is needed. Repos that aren't cloned yet are skipped -- see
\code{\link[=clone_repos]{clone_repos()}}.
}
