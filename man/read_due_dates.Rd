% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/github.R
\name{read_due_dates}
\alias{read_due_dates}
\title{Read assignment deadlines from a course site's schedule.csv}
\usage{
read_due_dates(path, time = "23:59:59", tz = Sys.timezone(), stub_map = NULL)
}
\arguments{
\item{path}{Path to the course site's \code{schedule.csv}, e.g.
\code{here::here("..", "2026-Fall", "schedule.csv")}.}

\item{time}{Time of day assignments are due, as \code{"HH:MM:SS"}. Defaults to
\code{"23:59:59"} (the 11:59pm deadline in the syllabus late policy).}

\item{tz}{Time zone for the deadlines. Defaults to the system time zone.}

\item{stub_map}{Optional named character vector mapping project stubs that
don't match their \code{assign} name after hyphen conversion.}
}
\value{
A tibble with \code{assign} and \code{due} (POSIXct) columns.
}
\description{
Builds an \code{assign}/\code{due} lookup for \code{\link[=check_late]{check_late()}} from the course website
repo's \code{schedule.csv}, which is the single source of truth for deadlines --
move a date there and the late check follows.
}
\details{
Handles the three deliverable families in that file's schema: homework
(\code{n_assign}/\code{due_assign}, named \code{hw1}, \code{hw2}, ...), mini projects
(\code{n_mini}/\code{due_mini}, named \code{mini1}, ...), and project deliverables
(\code{stub_project}/\code{due_project}). Column groups that are absent are skipped.

Project stubs on the site are hyphenated (\code{final-analysis}) while the
\code{assign} names in \code{pars.R} use underscores (\code{final_analysis}), so hyphens are
converted automatically. Use \code{stub_map} for anything that needs more than
that, e.g. \code{c(presentation = "final_presentation")}.

Instructor-run work (quizzes, exams, interviews, participation) has no repo
submission and so gets no deadline; those simply never appear here.
}
